import diff from "./diff";
import { h } from "./vdom";
import { ELEMENT_OBSERVER, ELEMENT_COLLECT } from "./constants";

export default {
    diff,
    h,
    ELEMENT_COLLECT,
    ELEMENT_OBSERVER
};
